name: livekalman_sdk
description: Reusable Dart gRPC SDK for the three-node Live Kalman platform.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  fixnum: ^1.1.1
  grpc: ^5.1.0
  protobuf: ^6.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0

